# Onboarding — reference frames

Frames from the founder's onboarding reference video (the authoritative target flow). The
sign-in frame has the founder's face + name blurred (public repo); the rest are app-setup
screens with no personal content. The activation coach-marks (step 1/2/3) are described in the
packet and are net-new [Design]. Authoritative behaviour is the app source cited by the packet.
